# Django Account Example
Just an example of Django login system<br>
Use ```python manage.py runserver``` to start server<br>
Create a superuser before visit /admin/<br>
```python3 manage.py createsuperuser```
